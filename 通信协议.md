# LBC 到 STM32

## 命令格式
27 CC <命令类型> <数据字节长度> <数据> <数据> ... <数据> AF

## 使能命令 00
数据字节长度：01

数据为 FF，打开单片机外设使能；为 00，关闭。使能状态映射到 `ENABLE`。

当单片机外设使能时，正常执行后续外设操作命令；

当单片机外设关闭时，忽略后续所有外设操作命令和配置命令，并令电机控制 PWM 占空比为校准占空比、灯光控制 PWM 占空比为 0、机械手舵机复位到原点。

## 看门狗命令 D9
数据字节长度：00

单片机内部维护一个看门狗系统，该系统由一个定时器组成，该定时器的初始状态为 0，单片机工作时累加；看门狗的值映射到 `WATCHDOG_CURRENT`。

当定时器累加到值 `WATCHDOG_UPPER_LIMIT` 时，停止使能所有设备。

当单片机接收到该看门狗命令时，将定时器置 0。

## 驱动平移命令 01
数据字节长度：04

数据部分应当被解读为两个 int16_t 的变量 `STEER_INTENSITY_X` 和 `STEER_INTENSITY_Y` 并寄存。

字节解读为数据时为小端序，下同。

int16_t 的变量，取值范围为 [-32768, 32767]，所以可以认为该命令对应 ROV 的 X 方向与 Y 方向位移强度分别为
```
I_x = STEER_INTENSITY_X / 32768
I_y = STEER_INTENSITY_Y / 32768
```

单片机按照 $I_x$ 与 $I_y$ 归一化后的强度驱动电机。

## 驱动转向命令 02
数据字节长度：02

数据部分应当被解读为一个 int16_t 的变量 `ROTATE_INTENSITY`。

int16_t 的变量，取值范围为 [-32768, 32767]，所以可以认为该命令对应 ROV 的转向强度为

```
I_r = ROTATE_INTENSITY / 32768
```

$I_r \in [-1, 1)$ 之间，$I_r$ 越接近 1，表明其顺时针旋转强度越大；$I_r$ 越接近 -1，表明其逆时针旋转强度越大。$I_r$ 为 0 则不旋转。

## 驱动浮沉命令 03
数据字节长度：02

数据部分应当被解读为一个 int16_t 的变量 `SINK_INTENSITY`。

int16_t 的变量，取值范围为 [-32768, 32767]，所以可以认为该命令对应 ROV 的下沉强度为

I_s = SINK_INTENSITY / 32768

$I_s \in [-1, 1)$ 之间，$I_s$ 越接近 1，表明其电机驱动下沉强度越大；$I_r$ 越接近 -1，表明其电机驱动上浮强度越大。$I_r$ 为 0 则不垂直驱动。

## 灯光命令 11
数据字节长度：04

数据部分应当被解读为两个 uint16_t 的变量 `LIGHT_INTENSITY_LEFT` 和 `LIGHT_INTENSITY_RIGHT` 并寄存。

将这两个 uint16_t 变量直接作为灯光驱动 PWM 的 CCR，驱动灯光。

## 机械手命令 12
数据字节长度：04

数据部分应当被解读为两个 uint16_t 的变量 `MECHARM_SERVO_1` 和 `MECHARM_SERVO_2` 并寄存。

将这两个 uint16_t 变量直接作为机械臂舵机驱动 PWM 的 CCR，驱动舵机。

## 配置命令 57
数据字节长度：09

数据字节有 9 字节，第一个字节指明要修改的是哪个量，后八个指明传入的值（不一定会用到 8 个，小端序编码，没用到的字节传入 00），第一个字节和要修改量的对应关系如下

- 00：看门狗上限，传入一个 uint16_t

- 01：第一个 PID 的 KP，传入一个 float
- 02：第一个 PID 的 KI，传入一个 float
- 03：第一个 PID 的 KD，传入一个 float
- 11：第二个 PID 的 KP，传入一个 float
- 12：第二个 PID 的 KI，传入一个 float
- 13：第二个 PID 的 KD，传入一个 float
...

此处第一个PID 第二个PID这个的顺序是指PitchInPID, PitchOutPID, RollInPID, RollOutPID, YawInPID, YawOutPID;

第三个PID 的命令是 21 22 23，后面的以此类推

- 61：PID 误差映射量 1pitch，传入一个 uint8_t
- 62：PID 误差映射量 2poll , 传入一个 uint8_t
- 63：PID 误差映射量 3yaw  , 传入一个 uint8_t

- 71：不调整角度，传入一个 uint8_t

## 询问命令 AA
数据字节长度：01

发送该命令给单片机后，单片机串口会回传消息，回传消息包含的信息格式如下

28 CD <数据类型> <数据字节长度> <数据> <数据> ... <数据> B0

依据用户发送的数据字节不同，回复的消息包含的信息也不同，如下是询问命令的数据字节和回复信息的关系

- 00：回复数据字节长度为 01，数据与 `ENABLE` 相同；
- 01：回复数据字节长度为 02，数据与 `WATCHDOG_CURRENT` 相同；
- 02：回复数据字节长度为 02，数据与 `WATCHDOG_UPPER_LIMIT` 相同；

- 10：回复数据字节长度为 02，数据与 `STEER_INTENSITY_X` 相同；
- 11：回复数据字节长度为 02，数据与 `STEER_INTENSITY_Y` 相同；
- 12：回复数据字节长度为 02，数据与 `ROTATE_INTENSITY` 相同；
- 13：回复数据字节长度为 02，数据与 `SINK_INTENSITY` 相同；

- 20：回复数据字节长度为 02，数据与 `LIGHT_INTENSITY_LEFT` 相同；
- 21：回复数据字节长度为 02，数据与 `LIGHT_INTENSITY_RIGHT` 相同；

- 30：回复数据字节长度为 02，数据与 `MECHARM_SERVO_1` 相同；
- 31：回复数据字节长度为 02，数据与 `MECHARM_SERVO_2` 相同；

回复数据类型和询问命令的数据字节相同，回传数据的数据类型和传入时相同。