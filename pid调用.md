# 原本的文件的pid调用

    原本文件的pid调用收到传感器接收数据的时间的影响 外环是100hz，内环是300hz
    同时他的单位是rad/s

# 现在的PID调用

    现在的PID 由于是直接传感器一问一答得到的数据，因此没有之前那个必须100 300 的频率限制。
    为了不需要大改动他的pid系数：
        内环是纯P(ki=kd=0)，kp与频率无关 → 300Hz掉到100Hz，kp不用换算
        外环有ki/kd，本来就是按100Hz调出来的 → 保持100Hz就能原样继承系数
    因此 我们把调用的频率都改成 100hz 100hz（内环、外环都每10ms各算一次）
    查询的频率因此也变成了100hz定时查询

