# 移植 HWT9053-485（维特 Modbus 姿态传感器）到 STM32F4 工程 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `STM32Core_SDK_9053Modbus` 模块里的协议栈（wit_c_sdk）移植进用户的 STM32F4 工程，让 F4 通过 485 以 >200Hz 读取 HWT9053-485 姿态传感器的全部 16 个寄存器数据，并提供给控制算法使用。

**Architecture:** 三层结构。协议层 = 原样拷贝 `wit_c_sdk.c/h` + `REG.h`（零修改）。驱动层 = 用 STM32F4 HAL 重写 UART：查询发送 + DMA/空闲中断收整帧、批量喂给 `WitSerialDataIn()`。应用层 = 新建 `imu_9053.c/h`：注册 SDK 回调、轮询查询、物理量换算、给控制算法提供干净的 `Imu9053_GetData()` API 和校准命令包装。

**Tech Stack:** STM32F4 + HAL 库（CubeMX 生成的项目结构，UART 用 DMA 接收 + 空闲中断）、C11、Keil MDK 或 CubeIDE。

## Global Constraints

- 源 SDK 文件（`wit_c_sdk.c`、`wit_c_sdk.h`、`REG.h`）**一行都不许改**，原样拷贝。
- 传感器波特率已由**官方上位机改为 115200 并保存**（不写代码改波特率）。代码里 UART 直接配 115200。
- 物理链路：F4 的 UART(TX/RX) ↔ RS485-TTL **自动方向模块** ↔ 9053-485 传感器。**没有方向控制脚，不要 PA1 翻转逻辑。**
- 查询固定读 `AX` 起 16 个寄存器（`WitReadReg(AX, 16)`），应答帧固定 37 字节。
- 从机地址 `0x50`，协议 `WIT_PROTOCOL_905x_MODBUS`（=5）。
- 目标刷新率 >200Hz（周期 ≤5ms），查询必须串行化：**上一帧应答解析完才发下一帧**，禁止重叠查询。
- 不移植：`UART1.c/h`（PC 调试命令通道）、`DIO.c/h`、`IOI2C.c/h`、`AutoScanSensor`（波特率已知）。
- 文件名与函数名：新建文件必须用本计划中给出的名字，函数签名一致。

---

## 第一部分：函数清单（逐文件逐函数，标注移植是否必需）

> 这是移植时的"对照表"。标记含义：**✓ 必需**（移植一定要有）、**◐ 可选**（有用但不是必须）、**✗ 不需要**（删除）。

### 文件 1：`wit_c_sdk/REG.h` — 寄存器地址字典

这个文件全是宏定义，没有函数。它给所有寄存器编号，`wit_c_sdk.c` 和换算代码都引用它。

**必需的数据寄存器（读取）：**

| 宏 | 地址 | 含义 | 必需 |
|---|---|---|---|
| `AX` / `AY` / `AZ` | 0x34/0x35/0x36 | 加速度 X/Y/Z | ✓ |
| `GX` / `GY` / `GZ` | 0x37/0x38/0x39 | 角速度 X/Y/Z | ✓ |
| `HX` / `HY` / `HZ` | 0x3A/0x3B/0x3C | 磁场 X/Y/Z | ✓ |
| `LRoll` / `HRoll` | 0x3D/0x3E | 滚转角低/高16位 | ✓ |
| `LPitch` / `HPitch` | 0x3F/0x40 | 俯仰角低/高16位 | ✓ |
| `LYaw` / `HYaw` | 0x41/0x42 | 偏航角低/高16位 | ✓ |
| `TEMP905x` | 0x43 | 温度 | ✓ |

**命令用寄存器（写）：** `KEY`(0x69) 解锁、`BAUD`(0x04) 波特率、`SAVE`(0x00) 保存、`CALSW`(0x01) 校准开关、`RRATE`(0x03) 输出频率、`RSW`(0x02) 输出内容、`BANDWIDTH`(0x1F) 带宽 —— 由 SDK 命令函数内部使用，应用层不直接碰。◐

**关键常量：** `WIT_PROTOCOL_905x_MODBUS`(5)、`KEY_UNLOCK`(0xB588)、`SAVE_PARAM`(0x00)、`WIT_BAUD_115200`(6)、`CALGYROACC`(0x01)、`CALMAGMM`(0x07)、`NORMAL`(0x00)。✓（前三个必需，其余命令常量由 SDK 内部引用，拷文件即自动带上）

**结论：REG.h 整个文件原样拷贝，无需挑选。** ✓

---

### 文件 2：`wit_c_sdk/wit_c_sdk.h` — 公共 API 声明（全部函数清单）

| 函数 | 作用 | 移植必需 |
|---|---|---|
| `WitSerialWriteRegister(SerialWrite)` | 注册"发送函数"。SDK 要发字节时调它。**移植必须调用，传入你自己的 UART 发送函数** | ✓ |
| `WitSerialDataIn(uint8_t)` | **数据入口**。串口每收到 1 字节就调它。**整个移植最容易漏的就是这个** | ✓ |
| `WitRegisterCallBack(RegUpdateCb)` | 注册"数据更新回调"。SDK 解析完一帧存好 `sReg[]` 后调用 | ✓ |
| `WitDelayMsRegister(DelaymsCb)` | 注册"延时函数"。SDK 内部等待（如命令间隔 20ms）时用 | ✓ |
| `WitInit(uint32_t 协议, uint8_t 地址)` | 初始化：设协议类型和从机地址。**必须在收发前调用一次** | ✓ |
| `WitDeInit(void)` | 反初始化，清空所有注册 | ✗ |
| `WitReadReg(uint32_t reg, uint32_t num)` | **发查询帧**：读从 `reg` 起 `num` 个寄存器。主循环每周期调一次 `WitReadReg(AX,16)` | ✓ |
| `WitWriteReg(uint32_t reg, uint16_t data)` | 发写帧：写单个寄存器。命令函数的底层 | ◐ |
| `WitStartAccCali(void)` | 开始加速度计校准（水平放置后调用） | ✓（控制精度需要） |
| `WitStopAccCali(void)` | 结束加速度计校准 | ✓ |
| `WitStartMagCali(void)` | 开始磁力计校准 | ◐ |
| `WitStopMagCali(void)` | 结束磁力计校准 | ◐ |
| `WitSetUartBaud(int32_t index)` | 改波特率（上位机已改好，可留作备选） | ✗ |
| `WitSetBandwidth(int32_t)` | 改带宽（滤波） | ◐ |
| `WitSetOutputRate(int32_t)` | 改输出频率（Modbus 模式无意义） | ✗ |
| `WitSetContent(int32_t)` | 改输出内容 | ✗ |
| `WitSaveParameter(void)` | 保存参数到传感器 Flash | ✓（校准后保存用） |
| `WitSetForReset(void)` | 软复位 | ✗ |
| `WitCaliRefAngle(void)` | 校准参考角 | ◐ |
| `CheckRange(...)` | 范围检查（内部辅助） | ◐ |
| `extern int16_t sReg[REGSIZE]` | **全局数据表**。SDK 解析结果全存这里，应用层换算从这里读 | ✓ |

**I2C/CAN 相关声明**（`WitI2cFuncRegister`、`WitCanWriteRegister`、`WitCanDataIn`）—— ✗ 不需要。

---

### 文件 3：`wit_c_sdk/wit_c_sdk.c` — 协议栈实现（内部逻辑）

| 函数 | 作用 | 移植处理 |
|---|---|---|
| `__CRC16(...)` | Modbus CRC16 计算（查表法） | ✓ 内部，拷文件自动带上 |
| `__CaliSum(...)` | 0x55 协议校验和（Modbus 不用） | 内部，留着无害 |
| `WitSerialWriteRegister` | 存发送函数指针 | ✓ |
| **`WitSerialDataIn`** | **核心接收状态机**：字节进 → 判断协议 → 拼帧 → 校验功能码/长度/CRC → 把寄存器写进 `sReg[]` → 调回调。Modbus 分支见 [wit_c_sdk.c:171-197](STM32Core_SDK_9053Modbus/Source/wit_c_sdk/wit_c_sdk.c#L171-L197) | ✓ 零修改 |
| **`WitReadReg`** | Modbus 分支生成 8 字节查询帧（[wit_c_sdk.c:351-366](STM32Core_SDK_9053Modbus/Source/wit_c_sdk/wit_c_sdk.c#L351-L366)）：`地址 0x03 寄存器高 寄存器低 数量高 数量低 CRC高 CRC低`。同时记下 `s_uiReadRegIndex` 供解析时定位 | ✓ 零修改 |
| `WitWriteReg` | Modbus 分支生成 8 字节写帧：`地址 0x06 寄存器高 寄存器低 数据高 数据低 CRC高 CRC低` | ◐ 零修改 |
| `WitInit` | 存协议号、从机地址，清零接收计数 | ✓ |
| `CopeWitData` | 0x55 协议帧分发（Modbus 不用） | 内部，留着无害 |
| 命令函数（校准/波特率/保存等） | 统一套路：`WitWriteReg(KEY, 0xB588)` 解锁 → 延时20ms → 写目标寄存器 | ✓ 拷文件即带 |
| `WitDeInit` | 清指针 | ✗ |
| `WitCanDataIn` / I2C 回调注册 | CAN/I2C 用 | ✗ 留着无害 |

**移植处理：整个文件原样拷贝。应用层只调 `WitInit`、`WitReadReg`、三个 Register 函数、以及需要的命令函数。** ✓

---

### 文件 4：`Source/APP/Main.c` — 应用层（参考写法，不直接拷贝）

| 函数 | 作用 | 移植处理 |
|---|---|---|
| `main()` | 初始化 → 注册 → 主循环查询/换算/打印 | ✓ 参考改写（去掉 printf，改为喂控制算法） |
| `CopeCmdData()` | UART1 调试命令解析（收 `x\r\n` 单字符命令） | ✗ |
| `ShowHelp()` | 打印帮助菜单 | ✗ |
| `CmdProcess()` | 执行调试命令 | ✗ |
| `RS485_IO_Init()` | 初始化 485 方向脚 PA1 | ✗ 你的自动方向模块不需要 |
| `SensorUartSend()` | 发送前拉高 PA1、发完拉低（[Main.c:223-228](STM32Core_SDK_9053Modbus/Source/APP/Main.c#L223-L228)） | ✓ **改写**：去掉方向翻转，就是"调你的 UART 发送函数" |
| `Delayms()` | 延时包装 | ✓ 改成 `HAL_Delay` |
| `CopeSensorData()` | 回调：按寄存器置分组旗子（[Main.c:235-271](STM32Core_SDK_9053Modbus/Source/APP/Main.c#L235-L271)） | ✓ **改写**：简化为"置新数据标志 + 释放查询锁" |
| `AutoScanSensor()` | 自动扫波特率 | ✗ 波特率已知 115200 |

**关键换算公式（抄进 `imu_9053.c`）：**

```c
fAcc[i]   = (float)sReg[AX+i]     / 32768.0f * 16.0f;    // g
fGyro[i]  = (float)sReg[GX+i]     / 32768.0f * 2000.0f;  // °/s
iBuff     = ((uint32_t)sReg[HRoll + 2*i] << 16) | (uint16_t)sReg[LRoll + 2*i];
fAngle[i] = (float)iBuff / 1000.0f;                      // °
fTemp     = (float)sReg[TEMP905x] / 100.0f;              // °C
```

---

### 文件 5：`Source/Drive/UART2.c` — 被替换的驱动（原样看，不拷贝）

| 函数 | 作用 | 移植处理 |
|---|---|---|
| `Usart2Init()` | 初始化 USART2（PA2/PA3 + PA1方向脚） | ✓ **改写**：用 CubeMX/F4 HAL 配置你的 UART @115200，无方向脚 |
| `USART2_IRQHandler()` | 每收到 1 字节调 `WitSerialDataIn()`（[UART2.c:51-60](STM32Core_SDK_9053Modbus/Source/Drive/UART2.c#L51-L60)） | ✓ **改写**：换成 HAL 的 DMA+空闲中断收整帧批量喂 |
| `Uart2Send()` | 阻塞发送（等 TXE/TC） | ✓ **改写**：`HAL_UART_Transmit` |

### 其余文件

| 文件 | 移植处理 |
|---|---|
| `UART1.c/h` | ✗ 不要 |
| `delay.c/h` | ✗ 用 `HAL_Delay` 替代 |
| `DIO.c/h`、`IOI2C.c/h` | ✗ 不要 |
| `STM32F10xLib/` | ✗ F4 工程用 HAL，不需要 |
| `MDK/` | ✗ 原工程 Keil 配置，不需要 |

---

## 第二部分：目标文件结构

在你的 F4 工程根目录下新建文件夹 `imu_9053/`，放 5 个文件：

```
<你的F4工程>/
├── Core/...                    # 已有的 CubeMX 生成代码
├── imu_9053/
│   ├── wit_c_sdk.c             # 拷贝，零修改
│   ├── wit_c_sdk.h             # 拷贝，零修改
│   ├── REG.h                   # 拷贝，零修改
│   ├── imu_9053.h              # 新建：应用层 API
│   └── imu_9053.c              # 新建：应用层实现
└── (CubeMX 生成的 uart.c 等)   # UART 外设在 CubeMX 里配
```

**UART 接线**：在 CubeMX 里把一个 USART 配成 **115200、8N1、使能 DMA RX（循环或普通模式）**，任意一个 USART 实例（下文用 `huart_imu` 代指，按你实际实例名替换）。

---

## 第三部分：实施任务

> 每个任务都以"Keil/CubeIDE 编译 0 错误 0 警告"为关卡（嵌入式没有 JUnit 单元测试，硬件验证步骤单独标注）。

### Task 1：拷贝 SDK 三件套并接入编译

**Files:**
- Create: `imu_9053/` 目录
- Copy: `imu_9053/wit_c_sdk.c`、`imu_9053/wit_c_sdk.h`、`imu_9053/REG.h`（从 `STM32Core_SDK_9053Modbus/Source/wit_c_sdk/` 拷贝）

**Interfaces:**
- Consumes: 无（纯拷贝）
- Produces: `wit_c_sdk.c/h`、`REG.h` 三个文件，供 Task 3 调用

- [ ] **Step 1: 拷贝文件**

```bash
# 在 F4 工程根目录下
mkdir imu_9053
cp <源>/Source/wit_c_sdk/wit_c_sdk.c imu_9053/
cp <源>/Source/wit_c_sdk/wit_c_sdk.h imu_9053/
cp <源>/Source/wit_c_sdk/REG.h      imu_9053/
```

（源路径 = `c:/Users/18524/Downloads/stm32F1/stm32F1/STM32Core_SDK_9053Modbus`）

- [ ] **Step 2: 加入工程编译**

Keil：在项目树里 `Add Existing Files...` 把 `wit_c_sdk.c` 加进去，Include Path 加上 `imu_9053/`。
CubeIDE：文件在 `imu_9053/` 下会自动被 CMake/项目扫描，确认 `.project` 包含该目录。

- [ ] **Step 3: 编译验证**

编译。预期：0 错误 0 警告。（若警告：通常是 `WIT_DATA_BUFF_SIZE` 等无影响，可接受，但不要改源码。）

- [ ] **Step 4: 提交**

```bash
git add imu_9053/
git commit -m "feat: import wit_c_sdk (905x modbus) into F4 project"
```

---

### Task 2：UART 驱动对接（发送 + DMA/空闲中断接收）

**Files:**
- Create: `imu_9053/imu_uart.h`
- Create: `imu_9053/imu_uart.c`
- Modify: 你的 UART 中断服务函数（CubeMX 生成，通常在 `stm32f4xx_it.c` 或 `uart.c`）

**Interfaces:**
- Consumes: Task 1 的 `WitSerialDataIn(uint8_t)`（来自 `wit_c_sdk.h`）
- Produces: `ImuUart_Send(uint8_t*, uint32_t)`、`ImuUart_RxStart(void)`、`ImuUart_FeedFrame(uint16_t)`；UART 中断服务函数里调 `ImuUart_FeedFrame`

**前提**：CubeMX 已把 UART 配成 115200，并开启 DMA RX（缓冲区 128 字节）+ 串口空闲中断。

- [ ] **Step 1: 写头文件**

```c
/* imu_9053/imu_uart.h */
#ifndef IMU_UART_H
#define IMU_UART_H

#include <stdint.h>

void ImuUart_Send(uint8_t *p_data, uint32_t uiLen);
void ImuUart_RxStart(void);
void ImuUart_FeedFrame(uint16_t len);   /* 由 UART 中断服务函数调用 */

#endif
```

- [ ] **Step 2: 定义发送函数和接收缓冲**

```c
/* imu_9053/imu_uart.c（新建） */
#include "main.h"            /* 你的 CubeMX 头，提供 huart_imu 句柄 */
#include "wit_c_sdk.h"
#include "imu_uart.h"

#define IMU_RX_BUF_SIZE 128   /* 大于37字节的应答帧即可 */

uint8_t g_imu_rx_buf[IMU_RX_BUF_SIZE];

void ImuUart_Send(uint8_t *p_data, uint32_t uiLen)
{
    HAL_UART_Transmit(&huart_imu, p_data, uiLen, 100);  /* 100ms超时 */
}

void ImuUart_RxStart(void)
{
    HAL_UART_Receive_DMA(&huart_imu, g_imu_rx_buf, IMU_RX_BUF_SIZE);
    __HAL_UART_ENABLE_IT(&huart_imu, UART_IT_IDLE);     /* 空闲中断 */
}

/* 由 UART 的 DMA 传输完成回调驱动，或直接在 IDLE 处理里批量喂 */
void ImuUart_FeedFrame(uint16_t len)
{
    for (uint16_t i = 0; i < len; i++)
        WitSerialDataIn(g_imu_rx_buf[i]);   /* 整帧批量喂给 SDK 解码 */
    ImuUart_RxStart();                       /* 重新武装接收 */
}
```

- [ ] **Step 3: 写 UART 中断服务函数（帧完成的唯一入口）**

```c
/* 你的 USARTx_IRQHandler 里追加： */
void USARTx_IRQHandler(void)
{
    if (__HAL_UART_GET_FLAG(&huart_imu, UART_FLAG_IDLE))
    {
        __HAL_UART_CLEAR_IDLEFLAG(&huart_imu);
        uint16_t len = IMU_RX_BUF_SIZE - __HAL_DMA_GET_COUNTER(&huart_imu.hdmarx);
        ImuUart_FeedFrame(len);
    }
    HAL_UART_IRQHandler(&huart_imu);
}
```

（`USARTx` 换成你实际的中断函数名，如 `USART1_IRQHandler`；`huart_imu` 换成你的句柄名。）

- [ ] **Step 4: 初始化时启动接收**

在你的初始化代码（或 `main()` 早期）里：

```c
ImuUart_RxStart();
```

- [ ] **Step 5: 编译验证**

编译 0 错误。**硬件验证（暂时用 SDK 的 printf 打印路径跳过）**：这一步先只确认编译通过；完整验证在 Task 3 数据出来后做。

- [ ] **Step 6: 提交**

```bash
git add imu_9053/imu_uart.c
git commit -m "feat: F4 uart driver with DMA+IDLE frame reception feeding wit_c_sdk"
```

---

### Task 3：应用层 `imu_9053.c/h` — 查询、换算、数据 API

**Files:**
- Create: `imu_9053/imu_9053.h`
- Create: `imu_9053/imu_9053.c`

**Interfaces:**
- Consumes: Task 1 的 `WitInit`/`WitReadReg`/`WitSerialWriteRegister`/`WitRegisterCallBack`/`WitDelayMsRegister`/`sReg`；Task 2 的 `ImuUart_Send`/`ImuUart_RxStart`；HAL 的 `HAL_Delay`
- Produces: `Imu9053_Data` 结构体、`Imu9053_Init()`、`Imu9053_Update()`、`Imu9053_GetData()`；供 Task 4 和用户控制算法调用

- [ ] **Step 1: 写头文件**

```c
/* imu_9053/imu_9053.h */
#ifndef IMU_9053_H
#define IMU_9053_H

#include <stdint.h>

typedef struct
{
    float acc[3];     /* g      */
    float gyro[3];    /* deg/s  */
    float angle[3];   /* deg    */
    float mag[3];     /* 原始量  */
    float temp;       /* 摄氏度 */
} Imu9053_Data;

void  Imu9053_Init(void);
void  Imu9053_Update(void);                 /* 每主循环周期调用：负责查询节拍与超时 */
int   Imu9053_GetData(Imu9053_Data *out);   /* 有新数据返回1并拷贝，无数据返回0 */

#endif
```

- [ ] **Step 2: 写实现（核心：查询串行化 + 换算）**

```c
/* imu_9053/imu_9053.c */
#include "imu_9053.h"
#include "wit_c_sdk.h"
#include "imu_uart.h"

static volatile int  s_query_inflight;  /* 1=已发查询等应答 */
static volatile int  s_new_data;        /* 1=有新一帧数据   */
static uint32_t      s_query_tick;      /* 查询发出时刻     */

/* SDK 回调：一帧解析完 → 释放查询锁 + 置新数据标志 */
static void OnSensorData(uint32_t reg, uint32_t num)
{
    (void)reg; (void)num;
    s_query_inflight = 0;
    s_new_data = 1;
}

/* SDK 延时回调 */
static void OnDelay(uint16_t ms)
{
    HAL_Delay(ms);
}

void Imu9053_Init(void)
{
    WitInit(WIT_PROTOCOL_905x_MODBUS, 0x50);
    WitSerialWriteRegister(ImuUart_Send);
    WitRegisterCallBack(OnSensorData);
    WitDelayMsRegister(OnDelay);
    ImuUart_RxStart();
}

void Imu9053_Update(void)
{
    if (!s_query_inflight)                          /* 上一帧应答已收完 */
    {
        WitReadReg(AX, 16);                         /* 发查询：读AX起16寄存器 */
        s_query_inflight = 1;
        s_query_tick = HAL_GetTick();
    }
    else if (HAL_GetTick() - s_query_tick > 10)     /* 超时10ms：丢帧，解锁重试 */
    {
        s_query_inflight = 0;
    }
}

int Imu9053_GetData(Imu9053_Data *out)
{
    if (!s_new_data) return 0;
    s_new_data = 0;

    for (int i = 0; i < 3; i++)
    {
        out->acc[i]   = (float)sReg[AX+i]     / 32768.0f * 16.0f;
        out->gyro[i]  = (float)sReg[GX+i]     / 32768.0f * 2000.0f;
        out->angle[i] = (float)((((uint32_t)sReg[HRoll + 2*i]) << 16)
                                | (uint16_t)sReg[LRoll + 2*i]) / 1000.0f;
        out->mag[i]   = (float)sReg[HX+i];
    }
    out->temp = (float)sReg[TEMP905x] / 100.0f;
    return 1;
}
```

> 说明：`Imu9053_Update` 里"应答一回来立刻发下一帧"，自然逼近 115200 下的最大吞吐（约 250Hz）；超时 10ms 兜底防丢帧死锁。
>
> 关于 `sReg[]` 读写竞态：`sReg[]` 由 UART 中断（后台）写入，`Imu9053_GetData`（前台）读取。由于查询严格串行（应答解析完才发下一帧），GetData 在 `s_new_data` 置位后读取，最多只会读到"前一帧或当前帧"，对控制算法是可接受的快照。若要求严格原子快照，可在 `Imu9053_GetData` 读 `sReg[]` 前加短暂临界区（关/开中断），本计划按可接受处理，不强制。

- [ ] **Step 3: 在主循环接入**

```c
/* 你的 main() while(1) 里： */
Imu9053_Init();           /* main() 开头只调一次 */

while (1)
{
    Imu9053_Update();                       /* 每周期推进查询 */
    Imu9053_Data d;
    if (Imu9053_GetData(&d))                /* 有新数据？ */
    {
        /* 你的控制算法在这里用 d.angle / d.acc / d.gyro ... */
    }
}
```

- [ ] **Step 4: 编译 + 硬件验证**

编译 0 错误。烧录后接上传感器，串口/调试器确认：数据按 >200Hz 刷新，`d.angle[0/1/2]` 随板子转动变化、`d.acc[2]` 静止时约 1g、`d.temp` 接近室温。（可用 `printf` 临时打印验证后删掉。）

- [ ] **Step 5: 提交**

```bash
git add imu_9053/imu_9053.c imu_9053/imu_9053.h
git commit -m "feat: imu_9053 app layer with 200Hz query loop and physical conversion"
```

---

### Task 4：校准命令包装（保留校准能力）

**Files:**
- Modify: `imu_9053/imu_9053.h`、`imu_9053/imu_9053.c`

**Interfaces:**
- Consumes: `WitStartAccCali`/`WitStopAccCali`/`WitSaveParameter`（Task 1 已带）
- Produces: `Imu9053_AccCaliStart()`、`Imu9053_AccCaliStop()`、`Imu9053_SaveParam()`

- [ ] **Step 1: 头文件加声明**

```c
void Imu9053_AccCaliStart(void);   /* 水平放置后调用 */
void Imu9053_AccCaliStop(void);
void Imu9053_SaveParam(void);      /* 校准完保存到传感器Flash */
```

- [ ] **Step 2: 实现（注意：改命令期间暂停查询，避免应答竞争）**

```c
void Imu9053_AccCaliStart(void)
{
    while (s_query_inflight);       /* 等当前一帧应答结束，485是半双工 */
    WitStartAccCali();
}

void Imu9053_AccCaliStop(void)
{
    while (s_query_inflight);
    WitStopAccCali();
}

void Imu9053_SaveParam(void)
{
    while (s_query_inflight);
    WitSaveParameter();
}
```

- [ ] **Step 3: 编译验证**

编译 0 错误。**硬件验证**：水平放置 → 调 `Imu9053_AccCaliStart()` → 等 2 秒 → `Imu9053_AccCaliStop()` → `Imu9053_SaveParam()`，断电重启后确认角度零偏被纠正、数据仍正常刷新。

- [ ] **Step 4: 提交**

```bash
git add imu_9053/imu_9053.c imu_9053/imu_9053.h
git commit -m "feat: calibration command wrappers"
```

---

### Task 5：集成验证（交付关卡）

**Files:** 无新文件。

- [ ] **Step 1: 全量验证清单**

- [ ] 编译 0 错误 0 警告
- [ ] 上电 2 秒内开始出数据（不需要按任何键）
- [ ] 实际刷新率 ≥200Hz（用计数器统计：1 秒内 `Imu9053_GetData` 返回 1 的次数）
- [ ] 转动板子，`angle` 三个值正确跟随
- [ ] 静止放置，`acc[2]` ≈ 1.0g，`gyro` 接近 0
- [ ] 拔掉传感器线 10 秒再插回，程序不卡死、数据自动恢复（验证超时重试逻辑）
- [ ] 校准流程走一遍并保存，断电重启后校准生效

- [ ] **Step 2: 提交（若还有未提交改动）**

```bash
git add -A
git commit -m "feat: integrate imu_9053 485 attitude sensor at >200Hz"
```

---

## 自检备注

- **不破坏 SDK**：三个 SDK 文件零修改，移植边界清晰，将来升级 SDK 只需覆盖这三个文件。
- **查询串行化**：`s_query_inflight` 保证 485 半双工下不重叠收发；10ms 超时兜底丢帧。
- **接收不丢字节**：DMA 整帧收 + 空闲中断，一帧只中断一次，不受逐字节中断影响。
- **已知取舍**：Modbus 一问一答模式在 115200 下理论约 250Hz；若以后要更高速率，改上位机把波特率提到 460800 后，把 CubeMX 的 UART 波特率同步改一下即可，代码不用动。
